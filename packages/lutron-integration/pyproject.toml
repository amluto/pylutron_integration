[project]
name = "lutron-integration"
version = "0.0.0"
description = "Lutron Integration Protocol client"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build >= 0.9.8, <0.10.0"]
build-backend = "uv_build"